"""
IDE installer package.

sg install --ide claude-code   → write hooks + MCP config for Claude Code
sg install --ide cursor        → write hooks + MCP config for Cursor
sg install --ide cline         → MCP config + rules block only
sg install --ide roo           → MCP config + rules block only
sg install --ide zed           → MCP config + rules block only
sg install --ide continue      → MCP config + rules block only
sg install --all-detected      → detect installed IDEs, run each installer
"""
